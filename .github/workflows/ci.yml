name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:

  test:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v4
    - run: node --test tests/scan.test.js tests/workflow.test.js

  actionlint:
    name: Lint workflow YAML
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v4
    - name: Install actionlint
      run: |
        VERSION=1.7.12
        curl -sL "https://github.com/rhysd/actionlint/releases/download/v${VERSION}/actionlint_${VERSION}_linux_amd64.tar.gz" \
          | tar xz -C /usr/local/bin actionlint
    - name: Run actionlint
      run: actionlint .github/workflows/code-review.yml .github/workflows/ci.yml
